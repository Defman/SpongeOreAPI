type: "object"
properties:
  id:
    type: "integer"
    format: "int64"
  createdAt:
    type: "string"
    format: "date-time"
  username:
    type: "string"
  roles:
    type: "array"
    items:
      $ref: "#/definitions/Role"
  projects:
    type: "array"
    items:
      $ref: "#/definitions/PluginId"
  stared:
    type: "array"
    items:
      $ref: "#/definitions/PluginId"